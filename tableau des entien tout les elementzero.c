#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int T[4];
	int i;
	for(i=0;i<4;i++)
	T[i]=0;
	for(i=0;i<4;i++)
	printf("T[%d]= %d\n",i,T[i]);
	return 0;
}
